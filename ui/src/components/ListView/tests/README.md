# Tests land here with the first pure helper.

# The composite shell currently owns no frappe-ui-free logic to unit-test;

# the first tests arrive with the composite `useListView`/serialization helpers.
