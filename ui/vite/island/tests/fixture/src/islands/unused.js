// The island does not import this module, so its classes must stay out of the
// island's sheet. A scan of the whole source tree, rather than of the island's
// own modules, puts every other screen's utilities in every island.
export const UNUSED = "text-ink-amber-7";
